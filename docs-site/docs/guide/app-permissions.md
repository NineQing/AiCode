# 软件权限

「设置 → 软件权限」集中管理 App 需要的系统级权限。权限状态在页面恢复时（包括从系统设置页返回）自动刷新，已授权的项点了不会有反应。

## 后台运行保活

开启后显示一个前台通知，避免 App 退到后台时进程被系统杀掉。即使没有任何终端会话、AI 也空闲，进程同样保持存活；进程被系统回收重建后会自动恢复这个常驻状态。点通知可以回到 App 主页。

跑编译这类长任务前建议打开。

::: tip AI 执行期间会自动保活
就算这个开关是关的，AI 开始执行任务时也会自动亮起同一个常驻通知并保持 CPU 唤醒（避免熄屏后回复断在一半、工具卡住），所有会话的任务都结束后自动收起。手动打开开关的意义在于“空闲时也常驻”：想让终端里自己跑的后台命令不被打断，就把它开着。开关开着时，AI 任务结束不会把它关掉。
:::

## 屏幕常亮

开启后只要 App 停留在前台，屏幕就不会自动息屏，适合盯着 AI 干活或看终端输出。切到其它 App 或回桌面后自动失效，不影响系统的自动锁屏设置，也不需要额外授权。

长时间开着会多耗电，插着充电器用更合适。

## Agent 完成通知

开启后，当 App 在后台、AI 完成一轮对话时会弹系统通知（标题「任务完成」，内容是你发的那条消息；如果是后台任务触发的自动轮次，显示对应的任务标题），点通知回到 App 看结果。

App 在前台时不弹，避免打扰你正在看的对话。

::: tip Android 13 及以上
首次开启上面两个开关时会先申请「通知」权限。如果系统弹不出授权框（权限之前被关过或拒绝过），会有弹窗提示，点「去设置」跳到系统设置页手动开启。权限授予前开关不生效。
:::

## 安装未知应用

显示当前是否已允许。未允许时点这一行会跳到系统设置里本应用的「安装未知应用」开关页，开启后才能安装 APK。

## 访问存储空间

显示当前是否已授权。未授权时点击会弹系统权限申请框，用于读写工作区文件与容器镜像。

如果权限已被永久拒绝（系统不再弹框），会有弹窗提示，点「去设置」手动开启。

## 忽略电池优化

显示当前是否已豁免。未允许时点击跳到系统的电池优化申请页，对本应用豁免省电策略，降低后台进程被杀的概率。

## 自启动管理

点击跳到你设备厂商（小米、华为、OPPO、vivo、魅族等）的自启动管理设置页，允许 App 在开机或被杀后由系统自动拉起。厂商设置页打不开或者匹配不到厂商时，会回退到本应用的详情页。

这一项配合「后台运行保活」和「忽略电池优化」一起用，能进一步降低后台被杀的概率——国产 ROM 上后台限制比较激进，跑长任务经常被打断的话，三个都开。

## Shizuku 授权

Shizuku 让本应用以 adb shell（uid 2000）身份执行系统命令，是 AI 的 `Shizuku` 工具的前提。这一行显示当前状态：

- **未安装 / 服务未运行**：点击打开 Shizuku 应用，按其中的指引安装或启动服务。
- **未授权**：点击申请授权，在 Shizuku 弹出的授权框里选「允许」。
- **已就绪**：授权完成，AI 即可调用 `Shizuku` 工具。

从 Shizuku 应用返回本页时状态会自动刷新。用法与安全注意事项见 [Shizuku 执行后端](/guide/shizuku)。
